# get-md

URL をブラウザで取得し、指定要素を Markdown に変換する CLI ツール。

## Tech Stack

- Rust (Edition 2024、MSRV 1.88 — let-chain を使用。依存 htmd 0.5.4 も let-chain により 1.88 以上を要求)
- headless_chrome (CDP 経由のブラウザ制御)
- htmd (HTML -> Markdown 変換、skip_tags/spacing オプション使用)
- clap (CLI 引数解析、derive feature)
- indicatif (プログレス表示)
- regex (日時パターンマッチング)
- url (相対URL -> 絶対URL 変換)
- anyhow (エラーハンドリング)

## Architecture

- WebDriver を使用せず、システムの Chrome/Chromium を CDP で直接制御
- JS レンダリング対応（SPA、動的コンテンツ）
- `--no-cache` で CDP の `Network.setCacheDisabled` によりブラウザキャッシュを無効化
- HTTPS 証明書は既定で検証し、信頼済みデバッグ用途のみ `--ignore-certificate-errors` で無視可能
- HTTP ステータスはページ内 JS ではなく CDP の Network response event から取得し、ページスクリプトによる偽装を防ぐ
- メインフローはセレクタ決定、ブラウザ起動/ページ読み込み、HTTP ステータス検証、HTML 抽出、Markdown 変換、出力処理の小さな関数に分割し、`BrowserPage` と `OutputWriteState` でブラウザ寿命と書き込み前状態を明示する
- CSS セレクタはブラウザ内 JS 実行により要素の outerHTML を取得する。セレクタ文字列を JS 文字列リテラルへ埋め込む際は `escape_js_string` で、改行/CR/タブは短縮エスケープ(`\n`/`\r`/`\t`)、NUL を含む U+0000〜U+001F の制御文字は `\uXXXX`、行区切り(U+2028)/段落区切り(U+2029)・引用符/バックスラッシュも個別エスケープしてから渡す。これにより制御文字を素通しすることによる CSS パーサ/CDP プロトコル層の予期しない挙動を防ぐ
- htmd で HTML -> Markdown 変換（script, style, noscript, svg は skip_tags で除去）
- Rust 側で相対 URL を絶対パスに変換（基準URLはレンダリング後の `document.baseURI` を使用し、`<base href>` とリダイレクト後URLに対応。Markdown リンク・画像の `[text](url)` パターン、`[text](<url>)` 形式、リンク先 URL 直前に空白がある形式、`\(` `\)` `\<` を含むリンク先の解析、`<...>` 内の `)` を終端として誤認しない解析、`<...>` 内の `\>` をエスケープとして正しく処理、通常リンク先URL内のクォート保持、`[text]( "title")` や `[text]( 'title')` のような空のリンク先 + title の保持、`\ ` を含む通常リンク先の解析に対応。標準形式リンク先の先頭 `<` はエスケープ判定（`\<`）を行い、エスケープされていない場合のみ山括弧形式の開始として扱う。先頭がバックスラッシュ列（`\\` や `\\\\` など）で始まるリンク先は、`<` が destination の先頭ではないため山括弧形式扱いせず、標準形式として処理する。`Url::join` 前に `\(` `\)` `\<` `\>` `\ ` を実文字へ戻してから絶対 URL 化する。絶対 URL 化後に `(` と `)` のバランスが崩れている場合は、標準形式の Markdown リンクが壊れないよう `<...>` 形式で出力する。インラインコード、コードフェンス内（ブロッククォート内のフェンスを含む）、CommonMark のインデントコードブロック内（ブロッククォート内を含む）は変換せず、未閉鎖のインラインバッククォートはリテラルとして扱い、実際のリンク/画像構文ではない単独の `](` は無視する。閉じ `)` が見つからない壊れたリンク候補や閉じ `>` がない壊れた山括弧リンク先があっても、走査を継続してネストしたリンクを含む後続の正常なリンクも解決する。リンク検出はコード領域を除外しつつ前方走査でエスケープと開き `[` をカウントする方式で、`\]` でエスケープされた閉じブラケット、`` \` `` でエスケープされたバックティック、インラインコード内に位置する `[` をリンクの一部として誤検出しない。長いバックスラッシュ列があってもエスケープ判定は O(n) で動作する。途中位置から再開する場合は再開位置直前の連続バックスラッシュ列をバイト末尾から逆走査して取り込み、再開時のエスケープ判定が常に正しく行われる（マルチバイト直後でも `\` バイト 0x5C は UTF-8 継続バイトと衝突しない）。`[![inner](img)](outer)` のように外側 `[` がカーソルより前にある場合は未閉鎖 `[` 数をループ間で引き継いで外側リンクも解決する）
- テーブルのセルパディングとセパレータダッシュを圧縮（コードフェンス内は変更しない）。セル内のエスケープ済みパイプ（`\|`）は区切りとして扱わず、セル内のインラインコードスパン（バッククォート列）内に含まれる `|` も区切りとして扱わない。テーブル本文の `--` や `:` のようなセパレータ風データセルも保持する。4 スペース以上インデントされた行は CommonMark のインデントコードブロックとして扱い、テーブル圧縮対象外（先頭インデントとセル内空白を保持する）
- フェンスコードブロックの開始・閉じ判定は CommonMark 仕様準拠。開始・閉じフェンスは先頭最大3スペースまでを許容し、4スペースインデントのバッククォート行はインデントコードとして扱う。閉じフェンスはマーカー以降が空白/タブのみの行であり、info string 付きの行（例: ` ```rust `）はフェンス内コードとして扱う。これによりフェンス内に再度フェンスマーカーが現れる Markdown でも、テーブル圧縮や URL 解決が誤って実行されない。`find_next_link_candidate` 側は `\n` で行分割するため line 末尾に CR が残り得るが、閉じフェンス判定は末尾 `\r` を line ending として無視し CRLF 入力でも閉じる。ブロッククォート内のフェンスでも同じ判定を適用する。CommonMark §4.5 に従い、backtick fence (`` ``` ``) の info string に追加のバッククォートが含まれる行（例: `` ``` `bad` ``）は開始フェンスとして扱わず、後続行は通常段落として処理する。tilde fence (`~~~`) にはこの制約は無い
- インラインコードはフェンスコードブロック境界を越えない。`has_matching_inline_code_closer` は次行のフェンス開始（ブロッククォート内含む）に到達した時点で「閉じ列なし」と判定するため、未閉鎖の `` ` `` 列はリテラル扱いとなり、後続行のリンクは通常通り URL 解決される
- 改行をまたぐインラインコードを走査する際は、各文字と閉じバッククォート列の消費後に物理的な行頭状態を更新する。これにより、行途中で閉じた後のフェンス風・4 スペースインデント風テキストを行頭構文と誤認せず、同一行・後続行のリンクを解決する
- 対応 OS: macOS, Windows

## Project Structure

```
src/
  main.rs       # CLI 定義、メインフロー、HTML 取得、Markdown 変換、出力処理
  progress.rs   # indicatif ベースのプログレス表示
Makefile        # build, release, test, fmt, check, install ターゲット
.github/
  workflows/
    ci.yml      # CI (test, clippy, fmt, MSRV 1.88, build)
    release.yml # リリース (バージョンバンプ、ビルド、GitHub Release、Homebrew更新)
```

## Development

```bash
make build    # デバッグビルド
make release  # リリースビルド
make test     # テスト
make check    # フォーマット確認 + clippy + check
make fmt      # フォーマット
make install  # /usr/local/bin にインストール
```

## Testing

- ユニットテストは Chrome 不要（CLI パース、セレクタ既定値、ブラウザ起動オプション生成、証明書エラー無視オプションのデフォルト安全性、HTTP ステータス検証ヘルパー、JS エスケープ、Markdown変換、URL解決、リンクパーサー、コード領域を除外する URL 解決、未閉鎖バッククォートを含むリンク解決、フェンスマーカー検出、テーブルセル分割、ファイル出力テキスト確定、出力前状態取得、通常ファイル出力の作成/上書き分岐、ファイルステータス判定（削除済み tracked ファイルの同内容復元、git 管理外パスの安全な未ステージ判定を含む実フロー再現）、`--ignore-date` 出力分岐（日時のみ差分は書き込みスキップ、日時以外の差分はアトミック書き込み）、日時無視比較、エスケープ判定、開き括弧検出、リンク候補検出、インラインコード閉じ検出、マルチバイト文字対応、フェンスコード直後のリンク解決、インラインコードとリンクの混在、プログレス表示（スピナー置き換え含む）、CRLF正規化、ネストフェンスのスキップ、画像リンク検出、リンク先分割の境界条件、空のリンク先 + title の保持と title 内括弧の閉じリンク誤認防止、単引用符 title の URL 解決と空リンク先保持、連続テーブル圧縮、連続山括弧リンク解決、URL解決のエラー分岐、無効なリンク先 URL の保持（標準・山括弧・title付き）、タイトル内エスケープ引用符、未閉鎖タイトル、ネスト括弧内の引用符、CRLFフェンスブロック、コロンのみセパレータ、セパレータ風データセル保持、行途中開始のインラインコード検出、テーブル行中央揃え・幅広パディング圧縮、タイムアウト境界値、ファイルステータス等値比較、ネストフェンス長さ不一致、多段相対パス解決、複数リンク候補検出、山括弧リンク先フォールバック、4段ネスト括弧、日時除去単独日付、無効日付パターン、日付バウンダリ境界、深ネスト引用符、altなし画像URL解決、双方非UTF-8日時比較、タブパディング圧縮、ブロッククォート内リンク解決、ブロッククォート内コードフェンスのURL解決除外、インデントコードブロック内リンクの URL 解決除外、時刻のみパターン除外、標準・山括弧混在リンク、タブ区切りリンク先、末尾バックスラッシュ単独リンク先、改行直後フェンス開始判定、リンクテキスト内括弧、フェンス直後空行テーブル圧縮、リンク先 URL 直前の空白を含む標準・山括弧リンクの URL 解決、基準 URL 由来のアンバランスな括弧を含む解決後 URL の山括弧化、ネスト画像リンクで基準 URL 由来のアンバランス括弧を山括弧化する回帰テスト、`\ ` `\(` `\)` `\>` を含むリンク先を絶対 URL 化する回帰テスト、壊れたリンク候補の後続リンク解決（標準・画像・山括弧・タイトル付きの4バリエーション）、壊れたリンク候補がネストしたリンクテキスト内にある場合の内側/外側リンク解決、閉じ `>` がない山括弧リンク先の後続リンク解決、閉じ `>` がない山括弧リンク先を終端済みリンクとして扱わない直接テスト、ブロッククォート対応フェンスマーカー検出関数の直接テスト（多段、空白なし、長いマーカー、空行、4スペース過剰インデントの拒否）、リンク先エスケープ展開関数の直接テスト（`\ ` `\(` `\)` `\<` `\>` 個別、複数連続、マルチバイト混在、エスケープ対象外バックスラッシュの保持、エスケープ対象外文字と混在するバックスラッシュの保持、連続バックスラッシュ後のエスケープ展開）、ブロッククォート行内テーブルが圧縮対象外であることの確認、ブロッククォート内フェンスコード内のテーブル行保持、`find_next_link_candidate` を途中位置から再開した際のエスケープ引き継ぎ確認（再開位置直前が奇数 `\` で `]` がエスケープされる場合・偶数 `\` で `]` がエスケープされない場合・`\[` が開き括弧としてカウントされない場合・`` \` `` がインラインコード開始としてカウントされない場合・マルチバイト直後の安全性）、閉じフェンス判定の CommonMark 準拠確認（マーカーのみ、末尾空白/タブのみ許容、末尾 CR を line ending として無視、info string 付きは閉じ扱いしない、開始フェンスより短い/長いマーカー、異なるマーカー文字、ブロッククォート内の閉じフェンス、4スペース過剰インデントの開始/終了フェンス誤認防止）、ブロッククォート対応閉じフェンス判定の追加境界テスト（末尾空白/タブのみ、末尾 CR の許容、開始より長い/短いマーカー、異なるマーカー文字）、インデント対応閉じフェンス判定関数（`is_closing_fence_line_after_indent`）の直接テスト（インデントなし、最大 3 スペース許容、4 スペース/タブインデント拒否、info string 付き拒否、末尾 CR 許容、開始より短い/同長以上のマーカー長判定）、CRLF 改行のフェンス後にリンクが解決される回帰テスト、ブロッククォート内 CRLF フェンス後にリンクが解決される回帰テスト、フェンス内の info string 付き行を閉じフェンスとして誤認しないことの回帰テスト（compact_markdown と resolve_markdown_urls 双方、ブロッククォート内のフェンスも含む）、URL パーレンバランス判定の直接テスト（バランス済み、なし、空、開き過剰、閉じ先行、ネスト）、URL 書き出しの直接テスト（バランス済みは括弧なし、アンバランスは山括弧、山括弧指定時は常に山括弧）、ブロッククォート記号剥がし関数の直接テスト（記号なし、単一/多段、スペースなし、行頭インデント、ブロッククォート間の複数空白、空行、記号のみ）、フェンス用ブロッククォート記号剥がし関数（`strip_fence_blockquote_markers`）の直接テスト（記号なし、単一/多段、記号直後スペースなし、行頭最大 3 スペース許容・4 スペース拒否、記号直後最大 3 スペース許容・過剰拒否、記号のみ、空行。`strip_blockquote_markers` と異なり CommonMark インデント規則で過剰インデントを None にする点を固定）、エスケープされた `<` が山括弧リンク先の開始として誤認されないことの回帰テスト（`find_link_close_paren` の直接テスト、標準形式リンク先での URL 解決、title 付き、山括弧形式内の `\<`、先頭がバックスラッシュ列で始まるリンク先 `\\<a` が標準形式として閉じ括弧位置を返すこと、`Url::join` が `\` を `/` 相当に解釈してオリジン直下へ解決すること）、アトミックなファイル書き込みのテスト（新規ファイル作成、既存ファイル上書き、欠けた親ディレクトリの作成、成功後に一時ファイルが残らないこと、Unix パーミッション保持、シンボリックリンク追従時のリンク保持と実体更新、dangling リンク先の新規作成、ハードリンクが意図的に切れること、出力先がディレクトリの場合のエラー、symlink ループの打ち切りエラー、相対 symlink の追従、多段 symlink チェーンの追従）、連続するセパレータ行の 2 本目が table_state=Body のため正規化されないことの確認、先頭に空白が無くクォートで始まるリンク先が標準形式の URL として扱われること、リンク先で空白の直後に非クォート文字が続く場合に後続のクォートを title 開始と誤認しないこと、改行をまたぐインラインコードを正しくスキップして後続リンクの `](` 位置を返すこと、4 スペース以上インデントされたテーブル風行・タブインデントされたテーブル風行が圧縮対象外として保持されること、3 スペースインデントは引き続きテーブル圧縮対象となる境界確認、インラインコードスパン（単一/二重バッククォート）内の `|` がセル区切りとして扱われないこと、未閉鎖バッククォートはコードスパン扱いせず通常通り分割されること、エスケープされたバッククォートはコードスパン開始として扱われないこと、`compact_markdown` 経由でインラインコード内 `|` を含む行が壊れず圧縮されること、CommonMark フェンスインデント剥がし関数の直接境界テスト（無インデント、1〜3 スペース、4 スペース拒否、先頭タブ拒否、スペース後タブ拒否、空行）、テーブルセパレータセル/行判定関数の直接テスト（ダッシュ/コロン許容、空セル・英数字・空白混入の拒否、行内に 1 つでも非セパレータがあれば false）、一時ファイルクリーンアップガード（`TempFileGuard`）の Drop 挙動の直接テスト（persisted=false で削除、persisted=true で保持）、一時ファイル生成（`create_temp_file`）の連続呼び出しでの一意性と指定親ディレクトリ直下への配置、出力先解決関数（`resolve_output_write_path`）が通常ファイルと存在しないパスを変更せず返すこと、`escape_js_string` が NUL/タブ/フォームフィード/バックスペース/その他 0x01-0x1F 制御文字をすべて短縮エスケープまたは `\uXXXX` に変換すること、backtick fence の info string に追加のバッククォートが含まれる行を開始フェンスと判定しないこと、tilde fence では info string にバッククォートを含めても通常通り受理すること、`has_matching_inline_code_closer` がフェンス開始行（通常およびブロッククォート内）に到達した時点で打ち切ること、フェンスより手前で閉じ列が見つかればそちらを優先すること、4 スペース過剰インデントの「フェンスもどき」は境界として扱わず探索を継続すること、backtick fence info string にバッククォートを含む擬似フェンス行があっても後続のリンクが解決されること、未閉鎖インラインコードと本物のフェンス開始が混在した入力でリンクが正しく解決されること、HTTP ステータス検証の境界値（399 は通過し 400 ちょうどで拒否）と 3xx リダイレクト許容・5xx サーバエラー拒否の分類）
- E2E テストは実際の Chrome/Chromium が必要（`#[ignore]` 付き）。GitHub Raw の実取得、ローカル `file://` ページでの相対 URL 解決、`<base href>` による基準URL解決、複数セレクタの `---` 結合、`--ignore-date` の日時差分のみの書き込み抑止と日時以外の差分の上書き、ページスクリプトが Performance API を偽装しても実 HTTP 404 を拒否することを確認する。一時ディレクトリは時刻・プロセス ID・プロセス内連番で一意化し、並列実行時の衝突を防ぐ
- 改行をまたぐインラインコードの回帰テストでは、閉じバッククォート列の直後にフェンス風テキストが続く場合、4 スペースインデント風テキストが続く場合、閉じ列が物理行頭にある場合の 3 境界を固定する
- `make test` または `cargo test` で実行

## Key Design Decisions

- Chrome/Chromium はシステムにインストール済みであることを前提とする
- HTTPS 証明書エラーは既定で無視しない。必要な場合のみ `--ignore-certificate-errors` で明示する
- セレクタ未指定時は body 全体を対象とする
- 複数セレクタ指定時は `---` で区切って結合
- ファイル出力時は末尾改行を保証
- ファイル出力は同一ディレクトリの一時ファイルへ書き込んでから `rename` でアトミックに置き換える（`atomic_write`）。書き込み中の I/O エラー（ディスク容量不足など）が起きても既存ファイルが切り詰め・破損しない。既存ファイルのパーミッションを保持し、書き込み権限を事前確認し、出力先がシンボリックリンクの場合は `read_link` でチェーンを解決して実体パスへ書き（dangling リンク先も新規作成）、リンクを保ったまま更新する。一時ファイルは `TempFileGuard` で失敗時に確実に削除し、`rename` 後は親ディレクトリを best-effort で fsync する（Unix）。`rename` は新しい inode で置き換えるため、出力先へのハードリンクは切れ ACL/拡張属性は引き継がれない（データ損失防止を優先した仕様）
- 完了表示は出力書き込み成功後にのみ表示する（✨ created / 📝 updated / ✔ unchanged）
- ファイルステータス判定: 新規→created、内容変更→updated、同一内容→unchanged。git管理下で未ステージ変更がある場合は常にupdated。既存ファイルの読み取りに失敗した場合はupdated。ファイルの存在状態と書き込み前の未ステージ状態は書き込み（`atomic_write`）前に記録し、書き込み後の `path.exists()` や diff 消失に依存しない。git 判定は対象パスに最も近い既存ディレクトリを起点に行い、削除済みの tracked ファイルや repo 外 cwd からの実行でも契約を守る
- `--ignore-date`: 日時パターン（`YYYY-MM-DD HH:MM(:SS)?`、スラッシュ区切り、`Z`・小数秒・タイムゾーン付き ISO 8601）を無視してファイル比較し、日時だけの差分なら上書きせず unchanged 扱いにする。双方に日時パターンを含む場合のみ比較し、非 UTF-8 や日時パターンを含まない場合は安全のため通常比較にフォールバックする。git 管理下の未ステージ変更がある場合は `file_status` と同じ契約で updated 扱い
- `idle_browser_timeout` は `timeout + 30s` のバッファを saturating 加算で設定する
- バージョニングは CalVer (YY.M.counter) 形式
